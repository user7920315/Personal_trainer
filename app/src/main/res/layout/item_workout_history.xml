<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Строка с данными тренировки -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingVertical="10dp"
        android:paddingHorizontal="4dp">

        <!-- Иконка упражнения -->
        <!-- android:id="@+id/tvWorkoutIcon" → генерирует R.id.tvWorkoutIcon -->
        <TextView
            android:id="@+id/tvWorkoutIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:gravity="center"
            android:textSize="20sp"
            android:background="@drawable/bg_stat_card"
            android:layout_marginEnd="12dp"/>

        <!-- Название + дата -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- android:id="@+id/tvWorkoutName" → генерирует R.id.tvWorkoutName -->
            <TextView
                android:id="@+id/tvWorkoutName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold"/>

            <!-- android:id="@+id/tvWorkoutDate" → генерирует R.id.tvWorkoutDate -->
            <TextView
                android:id="@+id/tvWorkoutDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#AAAAAA"
                android:textSize="12sp"
                android:layout_marginTop="2dp"/>

        </LinearLayout>

        <!-- Повторения -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <!-- android:id="@+id/tvWorkoutReps" → генерирует R.id.tvWorkoutReps -->
            <TextView
                android:id="@+id/tvWorkoutReps"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#E94560"
                android:textSize="18sp"
                android:textStyle="bold"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="повт."
                android:textColor="#666666"
                android:textSize="10sp"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Разделитель между элементами -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#1AFFFFFF"/>

</LinearLayout>